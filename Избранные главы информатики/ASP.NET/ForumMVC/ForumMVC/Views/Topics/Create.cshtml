@model ForumMVC.Models.TopicEdit

@{
    ViewBag.Title = "Новая тема";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-xs-12">
        <h3>@ViewBag.Title</h3>
@using (Html.BeginForm()) {

    @Html.ValidationSummary(true, "", new { @class = "alert alert-warning" })
    
    <div class="form-group">
        @Html.LabelFor(model => model.Name, new { @class = "control-label" })
        @Html.TextBoxFor(model => model.Name, new { @class = "form-control" })
        <span class="help-block">
            @Html.ValidationMessageFor(model => model.Name)
        </span>
    </div>
    <div class="form-group">
        @Html.LabelFor(model => model.Content, new { @class = "control-label" })
        @Html.TextAreaFor(model => model.Content, 5, 50, new { @class = "form-control" })
        <span class="help-block">
            @Html.ValidationMessageFor(model => model.Content)
        </span>
    </div>
    <input type="hidden" value="-1" name="Id" />
    <input type="hidden" value="@ViewBag.Rubric.Id" name="RubricId" />
    <input type="submit" value="Сохранить" class="btn btn-default" />
}

    <p>
        <a href="~/Topics/List/@ViewBag.Rubric.Id">К списку тем</a>
    </p>
    </div>
</div>

@section scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
